// Empty initial migration — TronBox requires a Migrations contract by default
// but Skills2CryptoEscrow is self-contained, so we keep this as a no-op to
// satisfy the TronBox migration runner.
module.exports = function (_deployer) {};
